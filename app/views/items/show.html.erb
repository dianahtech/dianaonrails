<div class="position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center bg-light">

<div class="col-md-5 p-lg-4 mx-auto my-0">      
<div class="card">
<div class="card-header pt-2">
<h2>Mostrar Item</h2>
</div>

<% @item.attributes.each do |attr_name, attr_value| %>
  <div>
   <strong><%= attr_name %></strong>:<%= attr_value %>
  </div>
<% end %>

<%= link_to 'Edit', edit_item_path(@item), class:"btn btn-secondary btn-lg btn-block mb-2"  %> 
<%= link_to 'Back', items_path, class:"btn btn-secondary btn-lg btn-block"  %>

</div>
</div>
</div>
</div>
